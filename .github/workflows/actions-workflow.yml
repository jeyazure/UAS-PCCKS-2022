name: GitHub Actions to Dockerhub
run-name: ${{ github.actor }} Run GitHub Actions to upload docker image 🚀
on: [push]
jobs:
  build-and-push-image:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    
    - name: build and tag image
      run: |
        docker build -t ACCESS_TOKEN/uas-pccks:latest -f Dockerfile .
    
    - name: push to dockerhub
      run: docker login -u ${{secrets.ACCESS_TOKEN}} -p ${{secrets.dckr_pat_oGF5YhY2rukv5m1k0aK08D2mcow}} && docker push ACCESS_TOKEN/uas-pccks:latest